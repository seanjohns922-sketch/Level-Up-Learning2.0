import type {StatsItem} from '@/data/assessments/revisions/level1StatisticaFiveForms';
export type StatsResponse={choice?:string;values:number[];leaves?:string[];touched:boolean};
export function emptyStatsResponse(item:StatsItem):StatsResponse{return {leaves:item.mode==='leaves'?item.stems!.map(()=>''):undefined,values:(item.mode==='list'||item.mode==='sort')?item.observations.map(()=>-1):item.initial?[...item.initial]:Array(item.target===undefined?item.categories.length:1).fill(0),touched:false};}
export function statsResponseReady(item:StatsItem,r:StatsResponse){return item.mode==='leaves'?r.touched&&r.leaves?.length===item.stems!.length&&r.leaves.every(v=>/^\d(?:[ ,]+\d)*$/.test(v.trim())):item.mode==='choice'?item.options.some(o=>o.id===r.choice):r.touched&&r.values.length===(item.answer as number[]).length&&r.values.every(v=>Number.isInteger(v)&&v>=0&&v<=((item.mode==='list'||item.mode==='sort')?item.categories.length-1:(item.responseMax??20)));}
export function scoreStatsResponse(item:StatsItem,r:StatsResponse){return statsResponseReady(item,r)&&(item.mode==='leaves'?item.leafAnswer!.every((row,i)=>row===r.leaves![i].trim().split(/[ ,]+/).join(' ')):typeof item.answer==='string'?r.choice===item.answer:item.answer.every((v,i)=>r.values[i]===v));}
export function statsExpected(item:StatsItem){return item.mode==='leaves'?item.stems!.map((stem,i)=>`${stem} | ${item.leafAnswer![i]}`).join('; '):typeof item.answer==='string'?item.options.find(o=>o.id===item.answer)?.label:(item.mode==='list'||item.mode==='sort')?item.answer.map(i=>item.categories[i].name).join(', '):item.answer.map((n,i)=>`${item.categories[item.target??i].name}: ${n}`).join('; ');}
