import {getRealmTheme} from '@/lib/useRealmTheme';
import Link from 'next/link';
const theme=getRealmTheme('space');
export default function SpaceExtensionPage(){return <main style={{background:theme.cardSurface,color:theme.accentTextSoft}} className="grid min-h-screen place-items-center  p-6 text-white"><section style={{borderColor:theme.borderRing}} className="w-full max-w-xl rounded-3xl border   p-8"><p className="text-sm font-bold uppercase tracking-widest ">Starpath</p><h1 className="mt-3 text-3xl font-black">Level 7–8 assessments</h1><p className="my-5 leading-relaxed">Each test has 30 questions. Take the pre-test first, then return for the post-test when your teacher is ready to check your growth.</p>{[7,8].map(level=><div key={level} className="mb-5"><h2 className="mb-3 text-xl font-bold">Level {level}</h2><div className="flex flex-wrap gap-4"><Link style={{background:theme.surfaceTint,borderColor:theme.borderRing,color:theme.accentText}} className="rounded-xl  px-5 py-3 font-bold text-stone-950" href={`/pretest?year=Year%20${level}&realm_id=space`}>Pre-Test</Link><Link style={{background:theme.surfaceTint,borderColor:theme.borderRing,color:theme.accentText}} className="rounded-xl border  px-5 py-3 font-bold" href={`/posttest?year=Year%20${level}&realm_id=space`}>Post-Test</Link></div></div>)}<Link style={{background:theme.surfaceTint,borderColor:theme.borderRing,color:theme.accentText}} className="mt-7 inline-block underline" href="/world">Back to my world</Link></section></main>;}
