import Link from "next/link";
import { redirect } from "next/navigation";
import { AdminPageHeading } from "@/components/admin/AdminPrimitives";
import { loadPlatformEngagement } from "@/lib/platform-admin-server";
export default async function PlatformAnalyticsPage(){const data=await loadPlatformEngagement();if(!data)redirect("/login");const {schools,thresholds}=data.snapshot;return <><AdminPageHeading eyebrow="Analytics" title="School engagement" detail={`Meaningful activity only. Strong ≥${thresholds.strong}% active in 7 days; Healthy ≥${thresholds.healthy}%; Inactive means no canonical activity for ${thresholds.inactiveDays} days.`}/><div className="border border-slate-200 bg-white shadow-sm overflow-x-auto"><table className="w-full min-w-[980px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr>{["School","Students","Active 7d","Active %","Lessons 7d","Quizzes 7d","Assessments 30d","Lessons / active","Last activity","Status"].map(h=><th key={h} className="p-4">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{schools.map(s=><tr key={s.schoolId}><td className="p-4 font-semibold"><Link href={`/admin/schools/${s.schoolId}`}>{s.schoolName}</Link></td><td className="p-4">{s.students}</td><td className="p-4">{s.active7d}</td><td className="p-4">{s.activePercent}%</td><td className="p-4">{s.lessons7d}</td><td className="p-4">{s.quizzes7d}</td><td className="p-4">{s.assessments30d}</td><td className="p-4">{s.lessonsPerActive}</td><td className="p-4 text-xs text-slate-500">{s.lastActivity?new Date(s.lastActivity).toLocaleDateString("en-AU",{timeZone:"Australia/Melbourne"}):"No activity"}</td><td className="p-4"><span className="border border-slate-200 px-2 py-1 text-xs font-bold">{s.status}</span></td></tr>)}{schools.length === 0 ? <tr><td colSpan={10} className="p-8 text-center text-slate-500">No active or trial schools are available for engagement reporting.</td></tr> : null}</tbody></table></div></>}
